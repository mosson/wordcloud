<%= form_for( shallow_args(@theme, @answer) ) do |f| %>
  <div class="field">
    <%= f.label :comment, "コメントを入力してください" %><br />
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit nil, class: "btn btn-primary" %>
  </div>
<% end %>
